services:

  db:
    image: ankane/pgvector:latest
    container_name: mcp-db
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: mcp-db
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root